spotless {
    java {
        removeUnusedImports()
        googleJavaFormat()
        licenseHeaderFile "$rootDir/gradle/license-header.java"
    }
    groovyGradle {
        target '**/*.gradle'
        greclipse()
        indentWithSpaces()
    }
}
